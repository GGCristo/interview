#include <catch2/catch_test_macros.hpp>

#include <person/patient.hpp>
TEST_CASE("Patient") {
  // std::unique_ptr<Person> p1 = std::make_unique<Doctor>("Pepe", 22, Person::Gender::male, Doctor::Specialty::Allergists);
  // SECTION("Negative age") {
    // REQUIRE_THROWS_AS(Patient("NONE", -10, Person::Gender::other, PersonCondition::criticalCondition), std::runtime_error);
  // }
}
